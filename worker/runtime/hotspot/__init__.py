"""上游热点：发现 → 筛选 → 推荐。

分层（与仓库其它域一致的切法）：

- :mod:`.models` —— 落库的事实形状，与上游 MCP 的 JSON 解耦
- :mod:`.mcp`    —— 与 ``stepwork-hotspot-mcp`` 的对接（进程外，P2）
- :mod:`.rank`   —— **纯函数**打分与排序（不碰 DB、不碰网络，故可全量单测）

为什么把「发现」和「推荐」分成两个命令：发现是取事实（换源不影响下游），
推荐是下判断（要吃品牌画像、历史选题、用户反馈）。合成一个命令，就没法
「换个角度重新推荐」而不重新抓一遍全网。
"""
