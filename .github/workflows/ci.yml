# STEPWORK CI Pipeline (Placeholder)
#
# 本文件为 Week 1 占位版本，仅声明 3 个 job 的结构。
# 真实命令将在主代理集成阶段（所有 Batch 完成后）启用。
#
# 触发策略：push 到 main + 所有 PR
# 并发控制：同一分支的新 push 取消旧 run

name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  rust-check:
    name: Rust Check (Tauri Host)
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Placeholder
        run: echo "W1.4 Tauri Rust Host not yet implemented. Real commands below are commented out."

      # 真实命令（主代理集成阶段启用）：
      # - name: Install Rust
      #   uses: dtolnay/rust-toolchain@stable
      #   with:
      #     components: clippy, rustfmt
      #
      # - name: Cache cargo
      #   uses: actions/cache@v4
      #   with:
      #     path: |
      #       ~/.cargo/registry
      #       ~/.cargo/git
      #       apps/desktop/src-tauri/target
      #     key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
      #
      # - name: cargo check
      #   working-directory: apps/desktop/src-tauri
      #   run: cargo check --all-targets
      #
      # - name: cargo clippy
      #   working-directory: apps/desktop/src-tauri
      #   run: cargo clippy --all-targets -- -D warnings
      #
      # - name: cargo fmt check
      #   working-directory: apps/desktop/src-tauri
      #   run: cargo fmt --all -- --check
      #
      # - name: cargo test
      #   working-directory: apps/desktop/src-tauri
      #   run: cargo test --all-targets

  frontend-check:
    name: Frontend Check (React + TS)
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Placeholder
        run: echo "W1.5 React Frontend not yet implemented. Real commands below are commented out."

      # 真实命令（主代理集成阶段启用）：
      # - name: Setup Node
      #   uses: actions/setup-node@v4
      #   with:
      #     node-version: '22'
      #     cache: 'npm'
      #     cache-dependency-path: apps/desktop/package-lock.json
      #
      # - name: Install dependencies
      #   working-directory: apps/desktop
      #   run: npm ci
      #
      # - name: TypeScript check
      #   working-directory: apps/desktop
      #   run: npx tsc --noEmit
      #
      # - name: Lint
      #   working-directory: apps/desktop
      #   run: npm run lint
      #
      # - name: Build
      #   working-directory: apps/desktop
      #   run: npm run build

  python-check:
    name: Python Check (Worker)
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Placeholder
        run: echo "W1.3 Python Worker not yet implemented. Real commands below are commented out."

      # 真实命令（主代理集成阶段启用）：
      # - name: Setup Python
      #   uses: actions/setup-python@v5
      #   with:
      #     python-version: '3.12'
      #
      # - name: Cache pip
      #   uses: actions/cache@v4
      #   with:
      #     path: ~\AppData\Local\pip\Cache
      #     key: ${{ runner.os }}-pip-${{ hashFiles('worker/pyproject.toml') }}
      #
      # - name: Install dependencies
      #   run: |
      #     python -m pip install --upgrade pip
      #     pip install -e worker[dev]
      #
      # - name: Ruff lint
      #   working-directory: worker
      #   run: ruff check .
      #
      # - name: Ruff format check
      #   working-directory: worker
      #   run: ruff format --check .
      #
      # - name: Type check (mypy)
      #   working-directory: worker
      #   run: mypy .
      #
      # - name: Pytest
      #   working-directory: worker
      #   run: pytest --cov=. --cov-report=xml --cov-report=term
      #
      # - name: Upload coverage
      #   uses: codecov/codecov-action@v4
      #   with:
      #     files: worker/coverage.xml
      #     fail_ci_if_error: false
