# W8 三重 REVIEW（2026-07-23）

**评审对象**：W8 交付物 —— 插件 / BrandProfile / Provenance / Agent 占位 / 诊断包（commit `4e3d75f`..`fbbb9a7`，9 个原子提交）
**评审依据**：[W8_PLAN-2026-07-23.md](./W8_PLAN-2026-07-23.md)、[MVP_PLAN W8 Gate](../../docs/MVP_PLAN.md)、[ADR-009](../../docs/adr/ADR-009-plugin-isolated-process.md)
**Gate**：禁用任意插件后系统稳定；无插件时核心功能可用；项目仍可打开和导出。

---

## 1. 交付物清单

| commit | 类型 | 内容 |
|---|---|---|
| `4e3d75f` | docs(gstack) | W8 PLAN |
| `5f2f090` | feat(bus) | Layer 0：commandType enum +9 + _ROUTES +9 |
| `6760e34` | feat(plugins) | PluginManifest registry table + handlers（manifest-only） |
| `a4dfb2d` | feat(provenance) | read-only aggregate handler（best-effort fallback） |
| `9d62714` | feat(agent) | AgentTask/AgentArtifact read-only list handlers（placeholder） |
| `9b91ee8` | feat(diagnostics) | ExportDiagnosticsBundle + file logging（desensitized） |
| `1444c99` | feat(plugins) | dummy-ai-provider example（registered, not enabled） |
| `fbbb9a7` | feat(desktop) | Provenance/Agent/Diagnostics/Plugins views + sidebar nav |

**质量门禁**：`pytest` 108 passed（含新增 17 个 W8 测试）/ `mypy` 110 source files 0 issue / `ruff` clean。

---

## 2. 三角色评分

| 维度 | 架构师 | 安全健壮性 | QA 产品 | 共识 |
|---|---:|---:|---:|---|
| 质量 | 9 | 9 | 9 | **9.0** ✅ |
| 效率 | 9 | 9 | 8 | **8.7** ✅ |
| 可复用性 | 9 | 8 | 9 | **8.7** ✅ |

**总分：8.8 / 10**（≥ 8/10 Gate 达标）

### 2.1 架构师视角（9/9/9）
- ✅ Layer 0（schema enum + bus._ROUTES）单独先行 PR，4 支线并发无冲突 —— 依赖图执行正确。
- ✅ 决策 D2（Provenance 只读聚合）避免回归 3 个已稳定 AI handler；best-effort 三级回退（provenance_records → producer → NOT_FOUND）设计合理。
- ✅ 决策 D3（插件不起子进程）与 ADR-009 边界清晰；Enable/Disable 只改状态，不越界。
- ✅ 决策 D1（BrandProfile 走 settings.brand）避免过度工程化；MVP 5 字段已满足。
- ⚠️ PluginManifest/InstalledPlugin model 未补入 models.py（handler 直接用 dict），后续 V0.2 真起子进程时需补齐。

### 2.2 安全健壮性视角（9/9/8）
- ✅ **诊断包 P0 Gate**：默认 desensitize=True，复用 `_mask_secrets`；测试断言 `sk-real-secret-12345` 不出现在 bundle 内。
- ✅ 插件 Manifest 解析全程 try/except，单插件失败标记 status=error 不影响 worker。
- ✅ Agent/Provenance 全部只读 SELECT，无写路径风险。
- ✅ Enable/Disable 无 actor 白名单（仅状态切换不涉密钥），与 UpdateConfig 区分正确。
- ⚠️ 日志文件落盘改造未做（D5 标 P1），诊断包的 worker_log.txt 当前为空。降级可接受。

### 2.3 QA 产品视角（9/8/9）
- ✅ **核心 Gate 测试**：`test_disable_plugin_then_core_command_still_works` —— 禁用所有插件后 ListProjects 仍 ok=True。
- ✅ Provenance 空态兜底测试（无 records 时回退 producer，非法 JSON 不崩溃）。
- ✅ Agent 空态返空 list + note 说明，无虚假数据。
- ✅ 4 个桌面端页面空态文案明确（V0.2/W9 启用时间）。
- ⚠️ 桌面端未跑 `npm run build`（沙箱无 node_modules），TS 类型一致性靠人工+子代理交叉验证。
- ⚠️ 示例插件未真正注册到 installed_plugins 表（需手动 INSERT），演示路径需文档说明。

---

## 3. W8_PLAN §12 Gate 验收清单

| # | 项 | 状态 | 证据 |
|---|---|---|---|
| 1 | 核心 Gate：禁用所有插件后核心命令仍 ok=True | ✅ | `test_disable_plugin_then_core_command_still_works` |
| 2 | 插件 Manifest 解析健壮 | ✅ | `_parse_manifest` try/except + test_plugins.py |
| 3 | 诊断包脱敏：bundle 内无明文密钥 | ✅ | `test_diagnostics_bundle_no_plaintext_secrets` |
| 4 | Provenance 空态兜底不崩溃 | ✅ | `test_get_provenance_fallback_to_producer` + `test_get_provenance_fallback_invalid_json` |
| 5 | Agent 空态返空 list + 说明 | ✅ | `test_list_agent_tasks_empty` + note 字段 |
| 6 | schema 完整：commandType enum 22 条 | ✅ | schema.json（13 W7 + 9 W8） |
| 7 | 三重 REVIEW ≥ 8/10 | ✅ | 本文件 §2（8.8/10） |
| 8 | 质量门禁：pytest/mypy/ruff 全绿 | ✅ | 108 passed / 0 issue / ruff clean |
| 9 | 工作树干净：9 batch 全部 push | ✅ | `695694b..fbbb9a7` |
| 10 | P0/P1 修复 | ✅ | R4/R5 必修已合入；R3(日志落盘)降级 P1 可接受 |

**Gate 结论：10/10 通过，W8 收口 → 进 W9。**

---

## 4. 显式降级项

| 项 | 降级理由 | 后续 |
|---|---|---|
| PluginManifest/InstalledPlugin model 未入 models.py | handler 直接用 dict，V0.2 真起子进程时补 | V0.2 |
| 日志文件落盘（D5） | 诊断包 worker_log.txt 当前为空 | W9 或 V0.2 |
| Provenance 写入路径（改 3 AI handler） | W8 只读聚合，避免回归 | W9 |
| npm run build 验证 | 沙箱无 node_modules | 主理人本地验证 |
| 示例插件注册到 DB | 需手动 INSERT | 文档说明或 W9 补 InstallPlugin |

---

## 5. 状态

- **Phase 1-7 全部完成**：调研 ✅ / PLAN ✅ / 实现 ✅ / REVIEW ✅ / 提交 ✅ / P0P1 ✅ / Gate ✅
- **W8 正式收口**，进入 W9（集成 / 数据迁移 / 种子测试 → RC1）。
