# W7 三重 REVIEW（2026-07-23）

**评审对象**：W7 交付物 —— CLI + MCP 共享命令总线（commit `90c0358`..`c23a35d`，7 个原子提交）
**评审依据**：[W7_PLAN-2026-07-23.md](./W7_PLAN-2026-07-23.md)、[MVP_PLAN W7 Gate](../../docs/MVP_PLAN.md)、[ADR-004/007/009](../../docs/adr/)
**评审方式**：三角色（架构师 / 安全健壮性 / QA 产品）独立打分，共识 P0/P1 已合入或显式降级。

---

## 1. 交付物清单（已 commit & push）

| commit | 类型 | 内容 |
|---|---|---|
| `90c0358` | docs(gstack) | W7 PLAN |
| `7ccc454` | fix(schema) | command-envelope enum +5 命令（R1 P0） |
| `7b421f5` | feat(worker) | run_command + build_envelope + 3 只读查询 handler |
| `7c4c4f9` | feat(cli) | stepwork-cli 5 子命令 |
| `1a78bef` | feat(mcp) | stepwork-mcp 5 只读 tool |
| `fb00725` | chore(pyproject) | console_scripts |
| `c23a35d` | chore(worker) | 清 6 个历史 mypy error（dev_bridge + resolve_tts） |

**质量门禁**：`pytest` 91 passed（含新增三端一致性 Gate 测试）/ `ruff` clean / `mypy` 102 source files 0 issue。

---

## 2. 三角色评分

| 维度 | 架构师 | 安全健壮性 | QA 产品 | 共识 |
|---|---:|---:|---:|---|
| 质量（设计正确性 / 可读性） | 9 | 9 | 9 | **9.0** ✅ |
| 效率（实现紧凑 / 无冗余） | 9 | 8 | 8 | **8.3** ✅ |
| 可复用性（三端接缝 / 后续扩展） | 9 | 9 | 9 | **9.0** ✅ |

**总分：8.8 / 10**（≥ 8/10 Gate 达标）

### 2.1 架构师视角（9/9/9）
- ✅ `run_command` + `build_envelope` 作为三端共享接缝，参数化 `source`/`actor_type`/`workspace_id`，与 TS `buildEnvelope` 形状一致 —— 三端一致性根基稳固。
- ✅ 修复了 Phase 3 的双重 parse bug（原 `app.py` 先 `parse_envelope` 又把 `env` 传给会再次 parse 的 `dispatch`）—— 提交前 catch 并修复，未流入主分支。
- ✅ MCP 5-tool catalogue 与 ADR-007 意图对齐；新增 3 只读 query 命令是"最干净满足 MCP 不越权 Gate"的路径。
- ⚠️ 唯一保留项：`build_envelope` 的 TS 与 Python 形状一致目前靠人工对齐，未做跨语言契约测试（P2，W8+ 评估）。

### 2.2 安全健壮性视角（9/8/9）
- ✅ **MCP 权威边界 = tool 注册集**：`update_config`/`UpdateConfig` 永不注册，写密钥路径根本不可达。actor 白名单是二道防线。
- ✅ **GetConfig 读开放给 agent**（返掩码 `••••` + `hasKey:bool`），修复了原总线把 GetConfig 与 UpdateConfig 一并限白名单的 bug —— 否则击穿 MCP `get_config` 真实路径。
- ✅ **CLI 密钥安全**：`config set` 互斥必填 `--file`/`--stdin`，绝不暴露 `--api-key`/`--secret`/`--token`/`--password`；测试断言选项字符串。
- ✅ `run_command` 顶层 `except Exception` 兜底为 `{"ok": False, "error": str(e)}`，调用方永不崩溃。
- ⚠️ CLI 一次性进程丢失 `CONFIG_OVERRIDES` 内存密钥 —— 文档明示限制（依赖 env / 持久化掩码 settings），不做代码修复（MCP 常驻无碍）。P2 保留。

### 2.3 QA 产品视角（9/8/9）
- ✅ **核心 Gate 测试**：`test_three_clients_produce_equivalent_command_result` —— 同命令经 CLI/UI/MCP 产出相同 `detail`（除 commandId/requestedAt 等不可比字段）。W7 polish 阶段补齐，字面断言三端一致性。
- ✅ MCP `tools/list` 恰 5 tool 且不含 `update_config`；`_TOOL_COMMANDS` 反查也不含 `UpdateConfig`（defense in depth）。
- ✅ `UpdateConfig` + `actor=agent` → `ok=False`；`GetConfig` + `actor=agent` → `ok=True`（返掩码）两用例齐备。
- ⚠️ MCP 真实 client 集成测试延后（W7 仅 stdio JSON-RPC 自测 + monkeypatch 隔离）—— W8+ 评估接入 Claude Desktop。
- ⚠️ CLI stretch 命令（import/transcribe/render/cancel）延后到 W8+ —— W7 先交付核心 5 条 + MCP 只读，符合 MVP_PLAN W7 范围。

---

## 3. W7_PLAN §12 Gate 验收清单（收口）

| # | 项 | 状态 | 证据 |
|---|---|---|---|
| 1 | 核心 Gate：同命令经 CLI/UI/MCP 产出相同 CommandResult | ✅ | `test_three_clients_produce_equivalent_command_result` |
| 2 | MCP 不越权：`tools/list` 恰 5 tool，`update_config` 不可达 | ✅ | `test_tools_list_returns_only_5_readonly_tools` |
| 3 | actor 白名单：`UpdateConfig`+agent 被拒；`GetConfig`+agent 返掩码通过 | ✅ | `test_run_command_update_config_agent_rejected` + `test_run_command_get_config_agent_allowed` |
| 4 | CLI 密钥安全：`config set` 仅 `--file`/`--stdin`，无明文参数 | ✅ | `test_config_set_exposes_file_and_stdin` |
| 5 | schema 完整：enum 13 命令齐全 | ✅ | `schemas/command-envelope.schema.json`（8 原始 + 5 新增） |
| 6 | 三端一致性根基：`build_envelope` 参数化 | ✅ | `worker/runtime/app.py:build_envelope` |
| 7 | 三重 REVIEW ≥ 8/10 | ✅ | 本文件 §2（8.8/10） |
| 8 | 质量门禁：pytest/mypy/ruff 全绿 | ✅ | 91 passed / 0 mypy issue / ruff clean |
| 9 | 工作树干净：6+1 batch 全部 push | ✅ | `541887f..c23a35d` 已 push origin/main |
| 10 | P0/P1 修复 | ✅ | §5 R1/R2/R3 + §9 P0/P1 全部合入；P2 显式降级（见 §4） |

**Gate 结论：10/10 通过，W7 收口 → 进 W8。**

---

## 4. 显式降级的 P2 项（不阻塞 W7，记入后续）

| 项 | 降级理由 | 后续 |
|---|---|---|
| TS/Python `build_envelope` 跨语言契约测试 | 当前靠人工对齐，三端一致性 Gate 测试已覆盖 Python 侧 | W8+ 评估 ts+py 联合测试 |
| CLI `CONFIG_OVERRIDES` 一次性进程丢失 | 文档明示限制；MCP 常驻无碍 | W8+ 若 CLI 高频用密钥再修 |
| MCP 真实 client 集成测试 | W7 仅 stdio 自测 + monkeypatch | W8+ 接入 Claude Desktop |
| CLI stretch 命令（import/transcribe/render/cancel） | 需本地文件路径 + ffmpeg | W8+ 按需补 |
| MCP 写类 tool（generate_topic/script/save） | 非纯只读，越权面需评估 | W8/W9 评估后再加 |
| ADR-007 tool schema 自动生成 | W7 手写 5-tool catalogue | W8+ 评估从 schema 自动生成 |

---

## 5. 状态

- **Phase 1-7 全部完成**：调研 ✅ / PLAN ✅ / 实现 ✅ / REVIEW ✅（本文件）/ 提交 ✅ / P0P1 修复 ✅ / Gate ✅
- **W7 正式收口**，进入 W8（插件 / BrandProfile / Provenance）。
